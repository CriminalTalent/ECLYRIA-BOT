# ECLYRIA 마법용품점 봇 - Gemfile
source 'https://rubygems.org'

# HTTP 클라이언트 (마스토돈 API 직접 호출용)
gem 'http'

# 환경변수 관리
gem 'dotenv'

# 구글 시트/드라이브 연동
gem 'google_drive'

# 구글 API 인증 (google_drive 의존성)
gem 'googleauth'

# JSON 처리 (Ruby 3.0+ 호환성)
gem 'json'

# 개발/디버깅용 (선택사항)
group :development do
  gem 'pry'      # 디버깅용
  gem 'irb'      # 대화형 Ruby
end